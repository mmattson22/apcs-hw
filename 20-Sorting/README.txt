Using the sort methods that we used in class on 12/8/14, we can see that since isort uses shift n times, using a smaller parameter each time, the runtime will be n

For the selection sort, the min has to be found at each n, so it takes a little longer.
